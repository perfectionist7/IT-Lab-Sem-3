DATA SEGMENT
A DB 0FEH
B DB 03H
ANS DW 0
DATA ENDS

CODE SEGMENT
ASSUME CS: CODE, DS: DATA
START:  MOV AX, DATA
        MOV DS, AX

        MOV AL, A
        IMUL B
        MOV ANS, AX

        MOV AH, 4CH
        INT 21H
CODE ENDS
END START
