DATA SEGMENT
A DW 0002H
B DW 0003H
ANS DD 0
DATA ENDS

CODE SEGMENT
ASSUME CS: CODE, DS: DATA
START:  MOV AX, DATA
        MOV DS, AX

        MOV AX, A
        MUL B
        MOV WORD PTR ANS, DX
        MOV WORD PTR ANS +2, AX

        MOV AH, 4CH
        INT 21H
CODE ENDS
END START

