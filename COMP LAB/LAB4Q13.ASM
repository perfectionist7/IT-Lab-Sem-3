DATA SEGMENT
A DW 0FFFFH
B DW 0001H
ANS DD 0
DATA ENDS

CODE SEGMENT
ASSUME CS: CODE, DS: DATA
START:  MOV AX, DATA
        MOV DS, AX

        MOV AX, A
        IMUL B
        MOV WORD PTR ANS, DX
        MOV WORD PTR ANS +2, AX

        MOV AH, 4CH
        INT 21H
CODE ENDS
END START
